import react from "react";

import * as stylex from "@stylexjs/stylex";

interface Props {
  label: string;
  checked: boolean;
}

export default function XIVRadioButton({
  label,
  checked,
}: Props) {
  return <div></div>;
}
